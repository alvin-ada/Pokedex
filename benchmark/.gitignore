# .gitignore - Benchmark 模块 Git 忽略文件
# 
# 🎯 作用：指定 Git 版本控制应该忽略的文件和目录
# 📱 模块：benchmark - 性能测试模块
# 🔗 功能：避免将构建产物和临时文件提交到版本控制

# 🔥 构建目录
# 📌 /build：构建过程中生成的所有文件
# 📌 内容：编译后的类文件、APK、测试报告等
# 📌 原因：这些文件可以重新生成，不需要版本控制
/build

# 💡 用法：保持版本控制的整洁，只跟踪源代码文件
# 📌 好处：减少仓库大小，避免合并冲突
# 📌 规则：只忽略可重新生成的文件